public class Floor extends Model {
  public Floor(gl, camera, light, shader, material, new Mat4(1), m {
    this.camera = camera;
    this.light = light;
    this.shader = shader;
    this.material = material;
    super.setModelMatrix

  }
}
